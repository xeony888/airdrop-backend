-- CreateTable
CREATE TABLE "Airdrop" (
    "id" TEXT NOT NULL,
    "owner" TEXT NOT NULL,
    "pkey" TEXT NOT NULL,
    "mintAddress" TEXT NOT NULL,

    CONSTRAINT "Airdrop_pkey" PRIMARY KEY ("id")
);
